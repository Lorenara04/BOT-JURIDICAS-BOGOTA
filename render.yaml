services:
  - type: web
    name: mi-bot-juridico
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    disk:
      name: auth-disk
      mountPath: /app/auth
      sizeGB: 1